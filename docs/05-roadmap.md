# แผนการพัฒนา

จาก Prototype → ระบบใช้งานจริง 3 สาขา

> ประมาณการเป็น **สัปดาห์-คน** (นักพัฒนา 1 คนทำเต็มเวลา)
> ถ้ามี 2 คน งานหลายส่วนทำขนานกันได้ เวลารวมจะสั้นลงประมาณ 35–40% (ไม่ใช่ครึ่ง)

---

## Phase 0 — ตัดสินใจก่อนเริ่ม

**ไม่ใช่งานเขียนโค้ด แต่ข้ามไม่ได้** — ทุกข้อกระทบการออกแบบ

### ✅ ตอบแล้ว (รอบแรก)

| # | คำถาม | คำตอบ | ผลต่อการออกแบบ |
|---|---|---|---|
| 1 | ใบกำกับภาษี — กระดาษ หรือ e-Tax? | **e-Tax Invoice** | ต้องมีใบรับรองอิเล็กทรอนิกส์ + ผู้ให้บริการที่สรรพากรรับรอง · เอกสารเก็บเป็น XML คู่กับ PDF · ดู `02-architecture.md` §10 |
| 2 | วันที่ในไฟล์ `110967fg.xls` | **อะไรก็ได้** | ระบบใช้ **`วันที่ใบรับ` (11 ก.ย. 2024)** เป็นวันเข้าสต๊อกเสมอ ซึ่งเป็นค่าที่ถูกต้องในทุกไฟล์ · ยังคงเตือนเมื่อ `วันที่เอกสาร` ต่างผิดปกติ |
| 3 | 3 สาขาเป็นนิติบุคคลเดียวหรือไม่ | **ยังไม่แน่ใจ** | ออกแบบรองรับทั้งสองแบบไว้แล้ว — ตาราง `branch` มี `tax_id` และ `branch_no` **รายสาขา** ถ้าเป็นนิติบุคคลเดียวก็ใส่เลขเดียวกันทุกสาขาแล้วต่างกันที่ `branch_no` · **ไม่ต้องรื้อทีหลัง** |

### ✅ ตอบแล้ว (รอบสอง)

| # | คำถาม | คำตอบ | ผลต่อการออกแบบ |
|---|---|---|---|
| 4 | เครื่องพิมพ์ | **laser A4** | CSS พิมพ์เป็น `@page{size:A4}` อยู่แล้ว · laser ทำสำเนาคาร์บอนไม่ได้ จึงพิมพ์ **ต้นฉบับ + สำเนา** เป็นคนละแผ่นในงานเดียว ติดป้ายมุมบนขวา |
| 5 | ไฟแนนซ์กี่เจ้า เรตเท่าไหร่ | **ยังไม่ทราบ — ใช้ค่าคร่าว ๆ ไปก่อน** | เรตเลิกเป็นค่าตายตัวในโค้ด · หน้าเคสสินเชื่อมีแท็บ **บริษัทไฟแนนซ์** เพิ่ม/แก้/ปิดใช้งาน/ลบได้ · เก็บ ดอกเบี้ย · ดาวน์ขั้นต่ำ · **ค่าคอมที่ร้านได้ต่อสัญญา** |
| 6 | โปรแกรมบัญชี | **Excel** | ส่งออก CSV มี BOM + CRLF เปิดใน Excel ภาษาไทยไม่เพี้ยน · เพิ่มปุ่ม **ชุดส่งบัญชี** เลือกงวดเดือน ออก 3 ไฟล์: ขาย (แยก VAT) · ค่าใช้จ่าย · รับเงิน (มีวันที่รับจริง) |
| 7 | ผู้ใช้พร้อมกัน + งบ | **10–20 คน · ไม่เกิน ฿1,000/เดือน** | อยู่กับ **Vercel Pro** ที่จ่ายอยู่แล้ว (฿0 ส่วนเพิ่ม) · **demo ฿40/เดือน** บน Supabase Free · **ใช้จริง ฿890/เดือน** เมื่ออัปเป็น Supabase Pro ซึ่งเป็นการกดเปลี่ยนแพ็ก ไม่ต้องย้ายข้อมูล (ดู `02-architecture.md` §3) |
| 8 | ราคา `DA6200` `BJKE00` `D13100` | **เคลียร์ออก · ผู้จัดการกรอกราคาเองทีละคัน** | มีปุ่ม **ตั้งราคาเคลียร์** ในลิ้นชักรถ (admin/manager) · โชว์ต้นทุน อายุสต๊อก และกำไรสดขณะพิมพ์ · **ขายต่ำกว่าทุนได้แต่ต้องยืนยันซ้ำ** · ระบบไม่เดาราคาให้เด็ดขาด |

### ⏳ ยังต้องตอบ

| # | คำถาม | ทำไมต้องตอบ |
|---|---|---|
| 9 | เรตไฟแนนซ์จริงของแต่ละเจ้า | ค่าที่ใส่ไว้ตอนนี้ (1.35 / 1.48 / 1.42 %/เดือน) เป็นค่าสมมุติ — ใส่ของจริงได้เองในระบบแล้ว แต่ตัวเลขในใบเสนอราคาจะยังไม่ตรงจนกว่าจะใส่ |
| 10 | ธนาคารที่ใช้จ่ายเงินเดือน | ไฟล์โอนเงินเดือนต้องตรงรูปแบบของธนาคารนั้น |
| 11 | ผู้ให้บริการ e-Tax | ต้องขอใบเสนอราคา 2–3 ราย พร้อมแจ้งปริมาณใบกำกับภาษีต่อเดือน (ดู `02-architecture.md` §9) |
| 12 | **ร้านย่อยมีกี่ร้าน · ชื่ออะไร · มีเลขสาขาสรรพากรแล้วหรือยัง** | เจ้าของยืนยันว่าร้านย่อย**ออกใบกำกับภาษีเองได้** แปลว่ามันคือ *สาขา* ไม่ใช่ *ที่เก็บของ* คำตอบทางเทคนิคคือ "เพิ่มแถวใน `BRANCHES`" ซึ่งได้สต๊อก การโอนย้าย และ `inScope` ทั้ง ~60 จุดมาฟรี — **แต่ยังไม่มีหน้าจอแก้สาขาเลย** และจำนวนสาขามีผลกับตัวนับเลขเอกสาร **อย่าเริ่มจนกว่าจะได้คำตอบ** (รายละเอียดที่ `08-state-and-handoff.md` §4) |

### ✅ ปิดแล้วในรอบ v1.13 – v1.15

| ข้อ | เดิมค้างอะไร | ปิดยังไง |
|---|---|---|
| 1 · ยืนยันตัวตนตอนเช็คอิน | เก็บพิกัดแต่ไม่เคยเทียบกับอะไร และเวลาที่บันทึกมาจากนาฬิกาของพนักงานเอง | จุดลงเวลาปักหมุดได้เอง (ร้านย่อยก็เป็นจุดหนึ่ง · ตั้งจากลิงก์ Google Maps ได้) · เวลามาจากเซิร์ฟเวอร์ · นอกรัศมีต้องบอกเหตุผลแล้วเข้าคิวตรวจ · ธงจับพิรุธ 6 ตัว **ข้อจำกัด: พิกัดยังมาจากเบราว์เซอร์ จึงเป็นมาตรการทางวินัย ไม่ใช่มาตรการความปลอดภัย** |
| 13 · API ให้เว็บขายรถ | RLS ทุกข้อเป็น `to authenticated` คนนอกได้ `[]` เปล่า | สคีมา `pub` + วิว + RPC + ถอนสิทธิ์ `anon` ใน `public` · **รัน migration ลงฐานข้อมูลจริงแล้วยิงตรวจครบทุก endpoint** · คู่มือ `docs/07-public-api.md` |

---

## Phase 1 — รากฐาน *(6–8 สัปดาห์)* 🔴 ต้องมีก่อนใช้จริง

เป้าหมาย: **ทำให้ระบบเก็บข้อมูลได้จริง ปลอดภัย และออกเอกสารได้ถูกกฎหมาย**

| งาน | เวลา |
|---|---|
| ตั้งโปรเจกต์ Next.js + Supabase (Free) + Vercel + CI/CD | 3 วัน |
| สร้างตารางทั้งหมดตาม `03-data-model.md` | 5 วัน |
| **เปิด RLS ทุกตาราง + ทดสอบว่ากันข้ามสาขาได้จริง** | 4 วัน |
| Login + บทบาท + **กรองเมนูตามสิทธิ์** | 4 วัน |
| Design system ตาม `04-design-system.md` เป็นคอมโพเนนต์จริง | 5 วัน |
| **ตัวนับเลขที่เอกสารแยกสาขา/ประเภท/ปี** | 2 วัน |
| นำเข้าไฟล์ยามาฮ่าจริง (`.xls` + normalize + กันซ้ำ + log) | 5 วัน |
| ตารางราคาจากไฟล์ราคายามาฮ่า + วันที่มีผล | 3 วัน |
| รับรถ · สต๊อก · ขาย (ครบทุกช่องที่ v0.5 ทิ้ง) | 6 วัน |
| **พิมพ์ใบเสร็จ / ใบกำกับภาษี ได้จริง** | 4 วัน |
| Dashboard ผู้บริหาร + Dashboard ขาย | 5 วัน |
| Audit log + งานอัตโนมัติ (Vercel Cron 5 งาน) | 3 วัน |
| ตั้งค่าระบบ (ข้อมูลบริษัท + ทุกเกณฑ์) | 2 วัน |
| ทดสอบ + แก้ + เตรียมข้อมูลจริง | 5 วัน |

**ส่งมอบ:** ระบบที่ **สาขาเดียว** ใช้ทำงานจริงได้ทั้งวัน — รับรถ ขาย ออกเอกสาร ดูภาพรวม

> ⚠️ **อัป Supabase เป็น Pro ก่อนออกใบกำกับภาษีใบแรก** — แพ็ก Free ไม่มีสำรองข้อมูลอัตโนมัติ
> การอัปเป็นการกดเปลี่ยนแพ็กที่ระดับ organization ข้อมูลและ URL เดิมอยู่ครบ ไม่ต้องย้าย

> 🎯 **นำร่องสาขาเดียว 2–4 สัปดาห์ก่อนขยาย** — สาขานำร่องจะเจอปัญหาที่คิดไม่ถึงเสมอ
> แก้ตอนมีสาขาเดียวถูกกว่าแก้ตอนมีสามสาขามาก

---

## Phase 2 — เติมสิ่งที่ขาด *(6–8 สัปดาห์)* 🟠

เป้าหมาย: **ครบตามที่เขียนไว้บนไวท์บอร์ด**

| งาน | เวลา | หมายเหตุ |
|---|---|---|
| **โมดูล Service** — ใบงาน · ค้นเลขเครื่อง/เลขถัง · ดึงประวัติลูกค้าเก่า · ตัดอะไหล่ · ค่าแรง · ปิดงาน | 8 วัน | **คุ้มที่สุด** — รายได้ซ้ำ + รักษาลูกค้า |
| ทะเบียน pipeline 6 ขั้น + เตือนค้าง | 4 วัน | *"the #1 thing managers ask"* |
| เคสสินเชื่อ — สร้างอัตโนมัติตอนขายผ่อน · เดินหน้า/ถอย/ปฏิเสธ · ผูกกับใบขาย | 4 วัน | |
| **เงินค้างรับจากไฟแนนซ์** + ลงรับเงิน | 4 วัน | |
| ใบเสนอราคา (รถ 2 คัน × ไฟแนนซ์ 2 เจ้า) + พิมพ์ | 4 วัน | |
| ค่าใช้จ่าย + หมวดหมู่ + **แนบไฟล์** | 4 วัน | |
| แนบบิล/ใบทะเบียนตอนรับรถ | 2 วัน | |
| เงินเดือน · OT · คอมมิชชั่น · สลิป | 6 วัน | |
| รายงานประกันสังคม + ไฟล์โอนธนาคาร | 3 วัน | ต้องรู้ธนาคารที่ใช้ |
| ลงเวลาจริง (แทนเวลาปลอมใน v0.5) + คิดสาย/OT | 3 วัน | |
| โอนย้ายรถระหว่างสาขา | 2 วัน | |
| Dashboard อะไหล่ + ไฟแนนซ์/ทะเบียน | 4 วัน | |

**ส่งมอบ:** ครบทุกโมดูล เปิดใช้ครบ 3 สาขา

---

## Phase 3 — เพิ่มคุณค่า *(4–6 สัปดาห์)* 🟡

| งาน | เวลา |
|---|---|
| รูปรถ + Stock Gallery แบบ LYY | 4 วัน |
| **LINE แจ้งเตือน** (เช็กระยะ · ทะเบียนออก · พ.ร.บ. ใกล้หมด) — **ออกแบบให้อยู่ในโควตาฟรี 300 ข้อความ/เดือน**: แจ้งพนักงานเข้ากลุ่ม · รวมหลายเรื่องเป็นข้อความเดียว · ใช้ rich menu ให้ลูกค้ากดดูเอง | 5 วัน |
| เตือนเช็กระยะตามเลขไมล์ ทำงานจริง | 3 วัน |
| ค้นหารวมทั้งระบบ | 3 วัน |
| โปรโมชันประจำสัปดาห์ + แถบเตือนเซลล์ | 2 วัน |
| ของแถมมีสต๊อกจริง + ตัดสต๊อกตอนขาย | 2 วัน |
| Dashboard ครบ 6 ตัวตาม permission matrix | 5 วัน |
| Export Excel/CSV ทุกตาราง | 3 วัน |
| **พ.ร.บ./ประกัน เป็นกรมธรรม์มีวันหมดอายุ** + เตือนต่ออายุ | 4 วัน |
| ปรับปรุงมือถือให้ใช้หน้างานได้ลื่น | 4 วัน |

---

## Phase 4 — เมื่อโตขึ้น 🟢

| งาน | เมื่อไหร่ |
|---|---|
| คิวออฟไลน์เต็มรูปแบบ | เมื่อพบว่าเน็ตล่มบ่อยจริง |
| e-Tax Invoice | เมื่อสรรพากรบังคับ หรือปริมาณเอกสารเยอะพอ |
| แอปมือถือแยก | เมื่อเว็บบนมือถือไม่พอ |
| หน้าลูกค้าดูข้อมูลรถตัวเอง | ต่อยอดจาก LINE |
| เชื่อมโปรแกรมบัญชี | ตามที่ตอบใน Phase 0 |
| ดึงยอดจ่ายไฟแนนซ์จากอีเมลอัตโนมัติ | หลังระบบนิ่ง — ของยาก ทำทีหลัง |

---

## สรุปเวลา

| Phase | เวลา | ผลลัพธ์ |
|---|---|---|
| 0 | 1 สัปดาห์ | ตัดสินใจครบ |
| 1 | 6–8 สัปดาห์ | 1 สาขาใช้จริงได้ |
| 2 | 6–8 สัปดาห์ | ครบทุกโมดูล · 3 สาขา |
| 3 | 4–6 สัปดาห์ | ครบตามที่ฝันไว้ |
| **รวมถึงใช้ครบ 3 สาขา** | **~13–17 สัปดาห์** (≈ 3–4 เดือน) | |
| **รวมทั้งหมด** | **~17–23 สัปดาห์** (≈ 4–6 เดือน) | |

---

## ความเสี่ยงที่ต้องเฝ้า

| ความเสี่ยง | ผลกระทบ | ลดความเสี่ยงยังไง |
|---|---|---|
| **ข้อมูลเดิมไม่สะอาด** | นำเข้าแล้วเละ แก้ทีหลังยาก | ทำความสะอาดตามกฎ `00-yamaha-export-analysis.md` §5 และ **ยืนยันเรื่องวันที่ก่อน** |
| **พนักงานไม่ยอมใช้** | ระบบดีแต่ไม่มีคนใช้ = เสียเปล่า | นำร่องสาขาเดียว · ให้คนหน้างานร่วมออกแบบ · เทรนก่อนเปิด |
| **ขอบเขตงานบานปลาย** | ไม่จบสักที | ทำ Phase 1 ให้ใช้ได้จริงก่อน ห้ามแทรก Phase 2 เข้ามา |
| **ใบกำกับภาษีผิดกฎ** | มีค่าปรับ | ให้บัญชี/ผู้สอบบัญชีตรวจแบบฟอร์มก่อนเปิดใช้ |
| **ข้อมูลหาย** | หยุดธุรกิจ | สำรองรายวัน + ดาวน์โหลด dump เก็บนอกระบบเดือนละครั้ง + **ทดสอบกู้คืนจริง** |
| **PDPA** | มีความรับผิด | ทำ consent + สิทธิ์ลบ/แก้ + log ตั้งแต่ Phase 1 |
| **คนทำมีคนเดียว** | ป่วย/ลาออกแล้วงานหยุด | เอกสารครบ · โค้ดอยู่ใน Git · ไม่มีอะไรอยู่แค่ในหัวคน |

---

## เกณฑ์วัดว่าสำเร็จ

| ตัววัด | ก่อน | เป้า |
|---|---|---|
| เวลาปิดการขาย 1 คัน (รวมออกเอกสาร) | ? | < 10 นาที |
| รู้ยอดขาย/กำไรของเมื่อวาน | ต้องรอสรุป | เปิดดูได้ทันทีตอนเช้า |
| **รถค้างสต๊อกเกิน 90 วัน** | **20 คัน · จม 887,100 ฿** | ลดลง 30% ใน 6 เดือน |
| รถรอทะเบียนเกิน 30 วัน | ไม่มีใครรู้ | เห็นทุกวัน · เป็นศูนย์ |
| เงินค้างรับจากไฟแนนซ์ | ตามเก็บด้วยมือ | เห็นยอดค้างทุกวัน |
| ลูกค้ากลับมาเช็กระยะ | ไม่ได้วัด | วัดได้ + เพิ่มขึ้น |
| เอกสารเลขซ้ำ | เป็นไปได้ | เป็นไปไม่ได้เชิงโครงสร้าง |

> ตัวเลข 20 คัน / 887,100 บาท มาจากไฟล์ยามาฮ่าจริงของคุณ (นับถึง 1 ส.ค. 2026)
> — ทั้งหมดอยู่ที่สาขา FMG01 และ 17 คันในนั้นรับเข้ามาตั้งแต่ 11 ก.ย. 2024 (689 วัน)

---

## อัปเดต 14 ส.ค. 2569 — บรีฟแก้ไขครั้งที่ 1 ส่งครบ

ทั้ง 6 กลุ่มของบรีฟ (เงิน/เอกสาร · ลูกค้า · บริษัท+B2B · พนักงาน · หน้าตา · แจ้งเตือน+เก็บงาน)
ลงต้นแบบ v1.21 และฐานข้อมูลจริงถึง migration 18b แล้ว — รายละเอียดต่อกลุ่มดู `docs/08-state-and-handoff.md` §2
สิ่งที่ค้างอยู่ฝั่งเจ้าของ (รูป/โลโก้/เรตจริง/เงื่อนไขเครดิต/งาน Supabase manual) ดู §3-§4 ของเอกสารเดียวกัน
