# สถานะระบบและการส่งต่องาน

เอกสารนี้ตอบคำถามเดียว: **ถ้าเปิดงานใหม่พรุ่งนี้ ต้องรู้อะไรบ้าง**
อัปเดตล่าสุด 31 ส.ค. 2569 · ต้นแบบ v1.49 (รวมสายแกลเลอรีต่อสี v1.31.1 แล้ว) · ฐานข้อมูลจริง migration 29 · **บรีฟแก้ไขครั้งที่ 1 ครบทั้ง 6 กลุ่ม
+ ผ่าน QA เจาะลึกรอบส่งมอบ** (บั๊กข้ามฟีเจอร์ 12 ตัว + จุดพังโหมดจริง 14 ตัว — แก้แล้ว มีด่าน qa-r22/live-r22 คุม)
v1.23: **สีประจำกลุ่มเดินตามหน้า** (แถบแท็บ · หัวกลุ่มเมนู · แถบล่างมือถือ) + แก้คลาส `g2`/`g3` ชนกับคลาสจัดหน้า
ที่ทำให้เมนูสองกลุ่มเรียงพังมาตั้งแต่ v1.20 — ดู `docs/04` §9k "สีกลุ่มเดินตามหน้า" ด่าน `grp-r23`
v1.24: **ช่วงเวลาเลือกเองได้ทุกหน้า** (1 วัน/7/15/1 เดือน/3/6/1 ปี + กำหนดเองแบบ agoda) —
เครื่องเดียว `perOf(key)` ใช้ทั้งหน้าแรก รายงาน ค่าใช้จ่าย ขายส่ง ใบเสนอ โอนย้าย อะไหล่ ดูแลหลังส่งมอบ ·
**ปุ่มส่งออกครบ 19 จุด** ทุกหน้าที่มีรายการ (ไฟล์ = สิ่งที่เห็นบนจอเสมอ) · ชุดส่งบัญชีออกตามช่วง ·
ด่าน `range-r24` (ผลรวมช่วงย่อย = ช่วงใหญ่ พิสูจน์ว่าไม่มีข้อมูลตกหล่น) + `export-r24` (ปุ่ม+สิทธิ์) ·
ตัวอย่าง Dashboard ที่ `docs/dash/` เปิดจากเว็บจริงได้
v25: เจ้าของเลือก**สไตล์เบนโตะ** — `docs/dash/` เหลือเบนโตะ **6 แบบ** ต่างกันที่ โทนสี · ความหนาแน่น ·
เรื่องที่เน้น · หน้าตากราฟ · **กราฟทุกตัวโค้งมนหมด** ตามคำสั่งเจ้าของ
v1.25: เจ้าของเลือก **"เอา 3 กับ 4 ผสมกัน"** → **หน้าแรกจริงเป็นเบนโตะแล้ว** (กริดเดียว งานค้างนำ
สต๊อกตาม โทนเย็น) การ์ดเดิมอยู่ครบ 12 ใบ + คิวงานตามวันครบกำหนด (`#dQueue`) กับเงินค้างรับแบบวงแหวน
(`#dAr`) ที่แยกออกมาใหม่ · สีกราฟเป็น token `--ch1..6` · `curvePath()`/`ringArc()`/`ringGauge()` ย้ายเข้า
`index.html` แล้ว · `vbar()` ถูกลบเพราะไม่มีใครเรียก — ดู `docs/04` §9m ด่าน `dash-r25`
`docs/dash/` เก็บไว้เป็นที่มาของดีไซน์ ไม่ได้ใช้รันจริง
v1.26: บรีฟ 5 ข้อ (18 ส.ค.) — ① กราฟหน้าแรกมีแกน x/y จริง + **กดดูตัวเลขได้ทุกกราฟ** (ป้ายลอย `#tip`)
② ทุกส่วนบนหน้าแรกกดดูข้อมูลเป็น**ป๊อปอัพ ไม่เด้งไปหน้าอื่น** และป๊อปอัพทั้งแอปเป็น**กล่องกลางจอ**
แนวเดียวกันหมดตามคำเจ้าของ ③ **ช่วงเวลา+สาขาขึ้น navbar** (จอกว้างชิปเต็ม มือถือปุ่มย่อ+แผ่นบน)
④ **ค้นหารวมค้นได้ทั้งหน้า/แท็บ/ข้อมูล** zero-state ลดการพิมพ์ + deep-link ผ่าน `go(k, sub)`
⑤ แก้ "ระบบลงชื่อเข้าให้เอง" — ต้นตอคือ seed สาธิตใส่เวลาเข้าวันนี้ ไม่ใช่ auto-punch: seed วันนี้ว่าง +
ด่านกันกดซ้ำใน `punch()` และ `punch_clock` (migration 20 ยิงพิสูจน์จริง 5 เคส) + โหมดจริงล้าง/โหลด `ATT` +
เตือนลืมลงเวลาออกถึงเจ้าตัว — ดู `docs/04` §9n · ด่านใหม่ 6 ชุด (punch/popup/chart/dashpop/navper/search -r26)
v1.26.1: แผ่นเลือกช่วงเวลา+สาขาบนมือถือ **โผล่ด้านบนตรงปุ่มที่กด** ไม่ใช่เด้งจากขอบล่าง (สายตาไม่ต้องกระโดดข้ามจอ)
v1.27: บรีฟ 2 ข้อ (18 ส.ค.) — ① **เลือกช่วงเวลาแล้วกดยืนยัน** จอไม่กระพริบระหว่างเลือกอีกต่อไป
(`perSync` + ธง dirty + `[data-pok]` เป็นที่เดียวที่วาดจอใหม่ + คืนค่าเดิมถ้าปิดแผ่นโดยไม่ยืนยัน) และ
**ปฏิทินอันเดียว** `#perCal` กดครั้งแรก=วันเริ่ม ครั้งที่สอง=วันจบ (จอคอม 2 เดือน มือถือ 1 เดือน · พ.ศ. เสมอ)
② **หน้า login ออกแบบใหม่ทั้งหน้า** เป็นสองฝั่ง (แบรนด์ซ้าย ฟอร์มขวา) · รายชื่อ 10 คนเห็นครบไม่ต้องเลื่อนในกล่อง ·
สีของหน้านี้ไม่ผูกกับธีม (`--login-fg`/`--login-sub`) · **ถอดช่องรหัสผ่านสาธิต `#lgPw` ที่ไม่มีใครอ่านค่า** —
ดู `docs/04` §9o · ด่านใหม่ 2 ชุด (`perpick-r27` · `login-r27`)
v1.27.1: ปุ่มบนแถบบนเปลี่ยนเป็น **"ยืนยัน"** และย้ายออกมาเป็นปุ่มของทั้งแถบ (`#navOk` ขวาสุด) —
กดครั้งเดียวเอา **ทั้งวันที่และสาขา** ไปกรองพร้อมกัน · สาขาเปลี่ยนเป็น "เลือกไว้ก่อน" (`NAV_BRP`)
และหน้าที่มีสาขาอย่างเดียว (สต๊อกรถ · ลูกค้าและดีล · การเข้างาน) เพิ่งมีปุ่มยืนยันครั้งแรก —
ดู `docs/04` §9o · ด่านใหม่ `navok-r27`
v1.27.2: **ปุ่มกระดิ่งแจ้งเตือนกลับขึ้นแถวบนสุด** อยู่ติดปุ่มค้นหาทุกขนาดจอ — ที่ 390 เคยตกไปแถวสอง
ลอยกลางจอเพราะแถวแรกกว้างไม่พอ (ต้องการ 412px มี 342px) · ปุ่มช่วงเวลาที่ลงไปแถวสองทำให้เต็มแถว
จะได้ไม่เป็นติ่งอีกอัน — ดู `docs/04` §9o · ด่านใหม่ `bell-r27`
v1.28: **เรียง flow ใหม่ทั้งระบบตามงานจริง** — คุยกับลูกค้า → ไฟแนนซ์ → เปิดการขาย → ส่งมอบ → ทะเบียนรถ
(เดิมเรียงตามลำดับของข้อมูล ไม่ใช่ลำดับของงาน) · ขั้นทะเบียนเรียงใหม่ให้ส่งมอบมาก่อนได้ป้ายจริง
เพราะลูกค้าใช้ป้ายชั่วคราวระหว่างรอขนส่ง · ดีลนับว่าจบตอนส่งมอบ ทะเบียนเป็นงานต่อท้าย
ผังกระบวนการ คู่มือ และหน้าสาธารณะเรียงตามกันหมด — ดู `docs/04` §9p · ด่านใหม่ `flow-r28`
v1.29: **ขอบเขตที่ดูเป็นบริษัท → สาขา** บน navbar — กดบริษัทก็บันทึกได้ (= ทุกสาขาในบริษัทนั้น)
หรือเลือกสาขาต่อก็ได้ · ค่าเดียวบอกสองระดับผ่าน `brMatch()` ไม่เก็บฟิลด์ที่สอง ·
ตัวกรองสาขาในหน้าถูกซ่อนหมด เหลือที่เดียวบน navbar · ข้อมูลสาธิตมีสาขาย่อยแล้ว 6 สาขา 3 บริษัท
และ**ทุกสาขามีทั้งรถและลูกค้าจริง** (รอบแรกกระจายชนจังหวะกันจนสาขาใหม่ไม่มีลูกค้าเลย — กับดักอยู่ใน §9q) —
ดู `docs/04` §9q · ด่านใหม่ `brpick-r29`

v30: **ตัวอย่างหน้าล็อกอิน 10 แบบ** ที่ `docs/login/` — ต่างกันที่**แนวคิดว่าหน้าล็อกอินมีไว้ทำอะไร**
ไม่ใช่ต่างแค่สี · แต่ละแบบมาพร้อมชุด token เต็มและ**หน้าแรกตัวอย่างในไฟล์เดียวกัน** เพราะเจ้าของสั่งให้
"ออกแบบเผื่อทั้งแอปไปเลย" · ทุกแบบรักษาสัญญาของหน้าล็อกอินครบ (`#lgGo` · `.lg-u[data-id]` 10 ตัวเรียง ST1..ST10)
จึงพอร์ตลงของจริงได้โดย**แก้ด่าน 0 ชุด** · ตรวจด้วย `tools/qa/gallery-check.js` (14 ข้อ อยู่นอก `suites/` เรียกมือ)
**ยังไม่แตะ index.html — รอเจ้าของเลือกก่อน** แล้วค่อยลงของจริงในรอบถัดไป · ดู `docs/login/README.md`

v1.30: บรีฟ 2 ข้อ (23 ส.ค.) — ① **แถบขั้นตอนสีเขียวโตขึ้นบนจอคอม** วงไอคอน 28 → 40px
(ไอคอน 22 · วงแหวน 6 · เส้นเชื่อมหนา 5 · ชื่อขั้น 13px) เฉพาะ `≥901px` ซึ่งเป็นเส้น "จอคอม" เดิมของแอป —
`.pstep` ไม่เคยมี media query เลยสักอันมาก่อน ขนาดที่ทำไว้ให้พอดี 390px ถูกใช้ซ้ำบนจอ 1440
② **ดอกจันแดงบนช่องที่จำเป็นต้องกรอกทุกฟอร์ม** — รายชื่อ `REQ` 49 ช่องประกาศที่เดียว

**v1.31–v1.39 (24 ส.ค.): บรีฟแก้ไขครั้งที่ 2 ครบทั้ง 30 ข้อ + บั๊ก 12 ตัว (B1–B12)** — ทำ 9 รอบ
commit ละรอบ ด่านใหม่ 9 ชุด (bugs-r31 … multirole-r39) mutation-proof ครบทุกข้อ:
v1.31 บั๊กเงิน/สิทธิ์ 6 ตัว · v1.32 ระบบรู้เงินสด/ผ่อนก่อนเปิดใบขาย + ฟอร์มบังคับก่อนยื่นไฟแนนซ์ ·
v1.33 ตัวเลือกรถไล่ระดับ (รุ่น→รหัส→สี→คัน + ค้น back-fill) + ตัวกรองรหัสรุ่นในสต๊อก ·
v1.34 แถบดีล 4 ขั้น (ตัดขั้นทะเบียนออก — งานป้ายเป็น `waitPlate`) + ไฟแนนซ์ 3 ขั้น
(ส่งเรื่อง→รอผลพิจารณา→อนุมัติแล้ว ชื่อเก่า normalize ตอนอ่าน) + ปุ่มส่งมอบเดียว+กล่องส่งมอบ
+ ขั้นย่อยใต้แถบ + อนิเมชัน (ปิดตาม prefers-reduced-motion) + คอลัมน์อัพเดทมี ชม.:นาที ·
v1.35 หน้า **ทะเบียนรถ** + บทบาทที่ 9 `reg` + จุดส่งต่อฝ่ายบริการย้ายไป**วันได้ป้าย**
(คำตอบเจ้าของข้อ 3 — careCreate อ่านวันจาก log จริง) + aftercare เป็นกระดานงานสด (ไม่กรองช่วงเวลา) ·
v1.36 หน้า **ใบกำกับภาษี** 3 ส่วน (ลูกค้า/ขายส่ง/อื่นๆ — ขายส่งย้ายจากหน้าขาย id เดิมทุกตัว)
+ เลขใบกำกับรันร่วม `nextTaxNo()` ซีรีส์ -TAX- ต่อบริษัทต่อปี + ด่านใบกำกับตัวรถออกหลังส่งมอบ
(DOWNTX ยกเว้น) + ใบวางบิล + ย้ายข้ามบริษัทจากหน้าโอนย้าย (tcGo→wsCommit แกนเดียว) + B8 B9 ·
**การขายใหม่เลิกจองเลขใบกำกับล่วงหน้า** — docNo เป็นซีรีส์ SALE ใบกำกับออกเลขตอนพิมพ์จริง ·
v1.37 หน้า **จองรถ** (hold เป็น reserved · มัดจำ optional + ใบเสร็จ · ยกเลิกคืนรถ+คืนมัดจำ ·
ด่านคันติดจองใน saveSale ขายได้เฉพาะคนจอง) + B4 จุดสีจองเป็น --info ·
v1.38 ประวัติไฟแนนซ์ต่อลูกค้า (c.finHist รอดข้าม void) + การ์ดอัตราผ่านไฟแนนซ์หน้าแรก
+ ยกเว้นมาสาย (waiveLate ตัดสินจุดเดียวใน attCalc + ด่าน act:hrApprove ใน attReview)
+ HR จอคอมสรุปขึ้นบนเต็มกว้าง + รายงานการขายละเอียด (แตะแถวเปิดรายเคส · เลขบัตรปิดกลางตามสิทธิ์)
+ ผังกระบวนการ lane เซลล์→ทะเบียน→บริการ + ผังสิทธิ์สร้างสดจาก PERMS ·
v1.39 **หลายตำแหน่งต่อคน** — สิทธิ์ union มากสุดชนะ (myRoles/rolePerm/perm จุดเดียว)
+ แอดมินแก้ตำแหน่งในหน้าบัญชีผู้ใช้ · migration 22–26 apply แล้วยิงตรวจ anon จริงทุกตัว ·
ของที่รอเจ้าของ: ระดับซีรีส์เลขใบกำกับ (ข้อ 7) · เซลล์พิมพ์ใบเสร็จ — ดู `.claude/issues/002`

**v1.40 (25 ส.ค.): แผงรายชื่อรุ่นใต้ช่องค้นหารถ** — เจ้าของ: "พอกดช่องนั้น จะมีรายชื่อรุ่นขึ้นมาให้กด"
แตะ `#svQ` (หน้าขาย) / `#bkQ` (หน้าจอง) → แผง `.sug` โชว์รุ่น + จำนวนคันจาก pool ของหน้านั้น
พิมพ์แล้วกรองชื่อรุ่น · ทำที่ `vehCascadeWire/Fill` จุดเดียวได้สองหน้า
v1.40.1: ป้ายเหลือ "ค้นหา" + **พิมพ์เลขเครื่อง/เลขถังแล้วเห็นคันที่แมตช์ขึ้นในแผงให้แตะเลือก**
(เดิมเติมเงียบแบบหยิบคันแรก — เจ้าของรายงาน "ข้อมูลไม่ขึ้น") · เติมเงียบเหลือเฉพาะแมตช์คันเดียว
— ดู `docs/04` §9u · ด่าน `modelsug-r40` (9 ข้อ · mutation 12 ตัว)

**v1.41 (26 ส.ค.): เลือกลูกค้าเดิมจากแผงในหน้าขาย** — แตะช่องชื่อ/เบอร์ว่างเห็นลูกค้าล่าสุด 8 คน
เลือกแล้ว **ทุกช่องที่มีข้อมูลถูกเติม** (ชื่อ เบอร์ วันเกิด เลขบัตร ที่อยู่) กรอกเฉพาะที่ยังว่าง
เลขบัตรเติมตามสิทธิ์ data:idNo (ไม่มีสิทธิ์ = placeholder ปิดกลาง ไม่ลง value — saveSale จึงไม่มีวัน
เขียนมาสก์ทับเลขจริง) · sCustFill จุดเดียวใช้ทุกทางเข้า · แก้บั๊กเดิม blur ปิดแผงข้ามช่อง
— ดู `docs/04` §9v · ด่านใหม่ `custpick-r41` (10 ข้อ · mutation 10 ตัว)

**v1.42 → v1.42.1 (26-27 ส.ค.): แถบการ์ดสต๊อกรายสี — เจ้าของขอกลับเป็นรายสาขา** —
ย้อนโค้ดเป็นแบบ v1.41 ทั้งก้อน · ของที่เหลือจากรอบนี้: ด่านกันรถนับเบิ้ลใน mcolor-r31 [11]
(ผลรวมเลขทุกการ์ด = จำนวนรถใน stList เป๊ะ) — ดู `docs/04` ท้าย §9v

**v1.43 (27 ส.ค.): การ์ดสต๊อกต่อรหัสรุ่น + ชิปสี · ภาพรวมลูกค้าแบบง่าย** — ① การ์ดแกลเลอรียุบ
21→14 ใบ (ใบละรหัสรุ่น) สีเป็นชิปจุดสีจริง+จำนวน จิ้มสลับรูปในการ์ด · กดการ์ดกรอง รุ่น+รหัสรุ่น
② กดรายลูกค้าเห็นภาพรวมสไตล์แอปลูกค้า: รูปใหญ่ + ชิป (ชำระ/ขั้นดีล/ทะเบียน) + รุ่น·สี·วันซื้อ +
จำนวนรถที่ผูก — ดู `docs/04` §9w · ด่าน mcolor-r31 [11][12][13] + picker-r33 [2] สัญญาใหม่
(mutation 5 ตัว) + ด่านใหม่ `dealhead-r43` (3 ข้อ · mutation 4 ตัว)
`markReq()` แปะดอกจันเข้า `label.fl` เรียกจาก `openModal()` + ท้าย `render()` + ตอนบูต ·
อีก 2 ช่องที่ไม่มีป้าย (เลขทะเบียน · ราคาต่อคันในตะกร้าขายส่ง) ฝังดอกจันไว้ในมาร์กอัป ·
ช่องที่บังคับ "บางกรณี" 4 ช่องจงใจไม่มีดอกจัน (บอกเงื่อนไขในป้ายแทน) —
ดู `docs/04` §9r · ด่านใหม่ `required-r30` (50 เคส **พิสูจน์ด่านจริงครบ 50/50** — เคลียร์ทีละช่อง
แล้วยิงฟังก์ชันบันทึกจริง ต้องได้ toast แดงที่ข้อความตรงกับด่านของช่องนั้น ไม่ใช่แค่ "แดงก็พอ")
+ แพตช์ `steps-r10` (ขนาดวงผูกกับความกว้างจอ)

**v1.49 (31 ส.ค.): เขียนกลับครบทุกตารางธุรกรรม (โหลดข้อมูลกลับ ครึ่งแรก — ทาง ก.)**
เจ้าของยืนยันขอบเขต "รวมโหลดข้อมูลกลับด้วย" · action ทุกตัวยิงลงฐานจริงแล้ว:
registration (+stage_log) · finance_case · receivable + receipt_payment · service_job ·
expense (+category ข้อความเสรี) · part/part_movement · freebie · quotation(+option) ·
follow_up_task (cadence + care) · service_reminder — id ใหม่เป็น uuid ในโหมดจริง ·
FK ที่อาจเป็น id เดโม guard ด้วย isUuid ที่จุดเรียก · เลขเอกสารโหมดจริงยิง RPC next_doc_no
ให้ตัวนับกลางเดินตาม (หลายเครื่องพร้อมกันรอโหลดตัวนับกลับใน v1.50) · ฟังก์ชันชื่อจริงเพิ่ม 3:
arReceive (ได้ด่าน canFinApprove ที่จุดเขียน) · svDeliver · dealTaskDone · migration 29 apply แล้ว
· ด่านใหม่ dbwrite-r49 (11 ข้อ) mutation 27 ตัวแดงตรงข้อ · ไม่มีการเปลี่ยน UI — ดู docs/04 §9ac
**ยังเหลือของครึ่งหลัง (v1.50)**: โหลดกลับตอนล็อกอิน + ตัวนับเลขเอกสาร + วิวตัดเงิน unit_v

**v1.48 (31 ส.ค.): ผสานจุดแข็ง grape ทาง ก. — ด่านสิทธิ์ครบ 8 ฟังก์ชันแกน + กันขายซ้ำระดับ DB**
ที่มา: รีวิวเทียบสองสายพัฒนา (repo grapethanapat147 คือ fork ที่ v1.15 สร้างใหม่เป็น Next.js) —
เจ้าของสั่งดึงจุดแข็งของสายนั้นเข้ามา · จุดที่ยืม: วินัย "ทุกฟังก์ชันเขียนเปิดหัวด้วยด่านสิทธิ์"
① ด่าน permW ใน `saveRecv` `saveSale` `svSave` `saveQuote` (ก่อน `nextDocNo` — โดนกันแล้วเลขห้ามถูกเผา)
· `expSave`/`partSave`/`partMove`/`giftSave` แตกจาก onclick เป็นฟังก์ชันชื่อจริง (แบบ `wsFinPass`)
— ของอะไหล่/ของแถมคีย์กับ `ptCanW(แท็บ)` เพราะสิทธิ์หน้านี้แยกรายแท็บ (§9h) · `voidSaleCore`
ได้ด่าน `canVoidSale()` + คืน boolean (ปิด issue #5) และเริ่มเขียน `sale.voided_at` ลงฐานตอนยกเลิก
② migration 28: `sale_unit_active_uniq` unique on `sale(unit_id) where voided_at is null`
(พอร์ตจากร่าง mig 20 ของสาย Next.js — กันสองเครื่องขายคันเดียวพร้อมกันที่โครงสร้าง DB)
+ ผูก `audit_changes()` เข้าตารางหลังแยกสาย (`wholesale_sale` `other_doc` `booking`)
· **ยิงพิสูจน์จริงแล้ว**: insert ขายซ้ำคันเดิมโดน `unique_violation` และ rollback สะอาด
③ ด่านใหม่ `guards-r48` (9 ข้อ) — mutation 9 ตัวแดงตรงข้อครบ (ตัวที่ [4] เคยเขียวเพราะข้อมูลทดสอบ
ไปสะดุด validation สาขาก่อนถึงด่าน — บทเรียน: ฟอร์มในด่านสิทธิ์ต้องถูกทุกอย่างยกเว้นสิทธิ์)
· จงใจไม่ทำรอบนี้: cron อัตโนมัติแบบ grape (ไม่มีเซิร์ฟเวอร์ — รอเจ้าของเลือกทาง pg_cron/Edge Function)
· เรตดอกเบี้ยรายช่วงงวดไม่ต้องผสาน — ของเรามีครบอยู่แล้ว (`finRate` tiers→rates→ฐาน)

**v1.47 (28 ส.ค.): แก้ "หลายส่วนที่ยาวเกินไป"** — เจ้าของ: "ช่องเลื่อนเลือกรถ ยาวจนผมเลื่อนไม่ไหว
... การ์ดในแต่ละหน้าที่มีหลายการ์ดจนทำให้หน้ายาวขึ้นเยอะ" · เลือก "แผงค้นหา + แสดงบางส่วน + ดูทั้งหมด"
**วัดก่อนแก้** (`body.scrollHeight ÷ innerHeight` ที่ 390): สต๊อก 5.3/ตาราง 6.3 · ดีล 5.8 · เงินเดือน 4.0
· บุคคล 3.7 ⇒ **หลังแก้ 2.6 / 1.9 · 3.3 · 3.3 · 3.1** (เพดาน 3.5 กลายเป็นด่านถาวร)
① หน้ากาก `.upick` บัง `<select>` เลือกคันรถทั้ง 4 จุด (`sUnit` `bkUnit` `tUnit` `wsUnit`) —
ปุ่มบอกคันที่เลือก + จำนวนคัน แตะแล้วเปิดแผงที่พิมพ์ค้นหาได้ · **select ยังมี option ครบและเป็นค่าจริง**
(ห้าม `display:none` · เลือกแล้วยิง `dispatchEvent('change')` ไม่ใช่เรียก `.onchange()`)
· ⚠️ `markReq` + `required-r30` ต้องไต่จาก `.upick`
② `cap` เป็นพารามิเตอร์ที่ 6 ของ `tbl()` — ตัดที่ **การวาด** เท่านั้น ข้อมูล/ตัวกรอง/ไฟล์ส่งออกครบเสมอ
· `#stTable` 12 · `#stGrp` 10 · `#stGal` 6 · `#dlTable` 8 · `#dlTasks` 6 · `#attTable` 8 · `#prTable` 6
— ดู `docs/04` §9aa · ด่านใหม่ `listcap-r47` (9 ข้อ · mutation 12 ตัว)
· **แก้ด่านเดิม 3 ชุด**: `required-r30` (ตัวไต่ป้าย) · `mcolor-r31` + `intent-r32` (กางก่อนนับ —
`intent-r32` เดิมเขียวเพราะบังเอิญ)

**v1.46 (28 ส.ค.): ป้าย "จองอยู่" + ตัวกรองการจองในหน้าลูกค้าและดีล** — เจ้าของ: "ลูกค้าจอง
จะขึ้นอยู่ส่วนไหนนอกจากหน้าจอง อยากให้ติดตามต่อได้ โดยไม่ทับกับการทำงานของลูกค้าทั่วไป"
`dealOf()` เพิ่ม `d.bk` **derive จาก BOOKINGS สดทุกครั้ง ไม่แช่เป็นฟิลด์** (ยกเลิกจองแล้วป้ายหายเอง)
· ป้ายต่อท้ายป้ายวิธีชำระในแถวเดิม ไม่เพิ่ม `<tr>` (deal-r10 ล็อก .pstep===แถว) ทั้งตารางและการ์ดมือถือ
· ตัวกรอง `#dlBook` (จองอยู่ / ไม่มีการจอง) ค่าเริ่มต้นไม่กรอง + เข้าแผ่นตัวกรองมือถือ
· ดีลรายคนบอกรถที่จอง มัดจำ วันที่จอง (อยู่ใน `.dlwho` ไม่ใช่บล็อกรูป — บล็อกรูปวาดเฉพาะคนที่มีใบขายแล้ว)
· `expDeal` เพิ่มคอลัมน์ "การจอง" — ดู `docs/04` §9z · ด่านใหม่ `dealbook-r46` (9 ข้อ · mutation 13 ตัว)
· **ไม่ต้องแก้ด่านเดิมสักชุด**

**v1.45 (28 ส.ค.): ยกเลิกบิลขายส่ง** — เจ้าของถาม "เปิดบิลขายส่ง รถตัดสต๊อกไหม · ยกเลิกบิลกดยังไง
ถ้าไม่มีให้ทำด้วย" + ตอบเพิ่ม "ยกเลิกได้ก่อนการเงินตรวจผ่านเท่านั้น" · **คำตอบ: ตัดสต๊อกจริง**
(ภายนอก `status='sold'` · ในเครือ `branch=dest` สถานะยังว่าง) และ**เดิมยกเลิกไม่ได้เลย**
ทำใหม่: `wsVoid(id,reason)` ด่าน 7 ชั้นในฟังก์ชัน (§9b) · `wsUnitBusy` กันเคสรถถูกใช้ต่อไปแล้ว
5 ทาง · `wsVoidModal` บังคับเหตุผล + เตือนเรื่องใบวางบิลที่ส่งไปแล้ว · `wsFinPass` ยกออกจาก
onclick อินไลน์ให้เรียกตรงได้ · `wsCommit` เก็บ `dest` (เดิมไม่จำปลายทาง) · กรอง void ที่รายงานภาษี
ชุดส่งบัญชี และการปิดคู่ค้า — **ไม่กรอง**ที่ `brRefs` และไฟล์ส่งออก (ตั้งใจ เขียนเหตุผลไว้ในโค้ดแล้ว)
— ดู `docs/04` §9y · migration 27 · ด่านใหม่ `wsvoid-r45` (12 ข้อ · **mutation 29 ตัว**)
· **จงใจไม่ทำ**: โหลดบิลกลับจากฐานข้อมูล (คำตอบเจ้าของ — ช่องว่างของทั้งระบบ ดู issues/001)

**v1.44 (28 ส.ค.): รูปรถจัตุรัสทุกจุด + ตัวครอปตอนอัปโหลด** — เจ้าของ: "ทุกส่วนที่มีรูปรถ แสดงผล
เป็นรูปขนาด 1:1 และเมื่ออัปโหลดรูป จะให้สามารถ crop รูปได้ ... ดูเป็นสากลและเหมาะสำหรับ mobile"
① ไฟล์เป็นจัตุรัสอยู่แล้วตั้งแต่ `imgResize` v1.14 — ที่ผิดคือกรอบ ⇒ แก้ CSS สองบรรทัด
(`.bph` 74×44 → `aspect-ratio:1` · `.gcard .ph` 4/3 → 1) + ลบ `height` จาก inline 3 จุด
ได้ 1:1 ครบ 8 จุด · (ตั้งใจลด `.gal` เป็น 150px มาชดเชยความสูง แต่วัดจริงแล้วมี media query
บังคับ 2 คอลัมน์ที่ ≤760px อยู่ก่อนแล้ว จึงย้อนออก — หน้าสต๊อกที่ 390 ยาวขึ้น 4,281→4,507 px)
② ตัวครอป `#crop` เป็นโฮสต์แยก z-index 63 นอก `#app` (ยืม `#mdB` ไม่ได้ — แผ่นแก้รุ่นใช้อยู่)
สถานะเป็นพิกัดในรูปต้นฉบับ · `cropDraw`/`cropFile` ใช้สูตร drawImage เดียวกัน ⇒ เห็นอย่างไรได้อย่างนั้น
· pan/zoom ด้วย Pointer Events + `touch-action:none` + บีบสองนิ้ว + สไลเดอร์ซูม + เส้นสามส่วน
— ดู `docs/04` §9x · ด่านใหม่ `crop-r44` (10 ข้อ × 2 จอ + ลากด้วยนิ้วจริง) + แก้สัญญา
`mcolor-r31` [3] (mutation 8 ตัว) · **`touch-action:none` จับได้ด้วย CDP `Input.dispatchTouchEvent`
บน context ที่ `hasTouch:true` — ลาก 120px ต้องขยับ 81.6px ถ้าถอด touch-action ออกเหลือ 13.6px**

v1.31: **หนึ่งสี = หนึ่งการ์ด · รูปผูกกับสี ไม่ใช่รุ่น** (23 ส.ค.) — เจ้าของ: "ตอนใส่รูป ต้องใส่รูปรถให้ตรงสี
พอเป็นให้เพิ่มรูปได้รูปเดียว แต่ใส่สีได้หลายบรรทัด จะทำให้สับสน" · `PRICE[รุ่น].c[รหัสสี]` เปลี่ยนจากสตริง
เป็น `{name, img?, imgUrl?, imgFullUrl?}` — **ไม่มีรูประดับรุ่นเหลืออยู่เลย** (จงใจ ไม่งั้นกลับมาสับสนว่า
รูปไหนชนะ) · `bikeArt()` รับรหัสสีเป็นอาร์กิวเมนต์ที่สาม หาสีจากรหัสก่อนแล้วค่อยเทียบชื่อ
เพราะรุ่นเดียวมีชื่อสีซ้ำได้จริง · แผ่นเพิ่ม/แก้รุ่นเป็นการ์ดต่อสี (รูป+รหัส+ชื่อ+ปุ่มเลือกรูป+เอาออก) ·
รายการรุ่นยุบคอลัมน์ "รูป"+"สี" เป็น **"สีและรูป"** สีละช่อง (เจ้าของเลือก "รุ่นละแถว โชว์รูปทุกสีเรียงกัน")
**ของแถมที่จำเป็น:** เจอว่ารูปที่อัปหายทุกครั้งที่รีเฟรช เพราะไม่เคยเขียนแถวลง `model_photo` เลย —
**migration 21** เพิ่ม `color_code` + เปลี่ยน unique เป็น `(variant_id, color_code, sort)` + FK ไป `model_color`
· `modelPhotoUpload()` เขียนแถวจริงและเก็บ path (ไม่ใช่ URL) · โหมดจริงโหลดสีและรูปกลับมาใส่ PRICE ·
`pub.model` ยกรูปเข้าไปใน `colors[]` — ดู `docs/04` §9s · ด่านใหม่ `mcolor-r31` (10 ข้อ · mutation 3 ตัว)
+ แพตช์ `photo-r14` `required-r30`
v1.31.1: **แกลเลอรีสต๊อกตามแบบที่เจ้าของส่งมา** — หนึ่งใบ = **รุ่น + สี** (รูปจึงตรงสีเสมอ · 14 ใบ → 21 ใบ)
รูป · เส้นคั่น · แบรนด์+จำนวนคันตัวโต · ชื่อรุ่น · `รหัสรุ่น · ชื่อสี` · ชื่อไทย+ปี ·
**แถบสัดส่วนสต๊อกตามสาขา** + พิลล์บอกสาขา+จำนวน (สีสาขาผูกกับลำดับใน BRANCHES ไม่ใช่ลำดับที่เจอ)
· การ์ดมีกรอบจริงแต่ยังเป็นกล่อง 2 ชั้น เพราะตัวการ์ดมีแค่ขอบ และ `#stGalWrap` เป็น `.bd flush`
· แก้ของที่เจอระหว่างทาง: พิลล์เคยมีจุดสองจุด (`.pill.pdot`) · ชื่อสาขายาวเคยถูกตัดหายที่ 390px
— ดู `docs/04` §9s · `mcolor-r31` ขยายเป็น 13 ข้อ · mutation 5 ตัว

Production: **https://famai-web.vercel.app** — Vercel ผูกกับ branch `claude/start-b18xi3` (push = deploy)
`.vercelignore` จำกัดของที่ขึ้นเว็บให้เหลือ `index.html` + `docs/manual/` + `docs/dash/` + `docs/login/` (ตัวอย่างให้เลือก) เท่านั้น

---

## 1. ของอยู่ที่ไหน

| อะไร | ที่ไหน |
|---|---|
| แอปทั้งตัว | `index.html` ไฟล์เดียว ~9,400 บรรทัด ไม่มี build step ไม่มี `package.json` |
| ฐานข้อมูลจริง | Supabase `famai-motor` ref `hpsmjavfvrdctclmlmhp` · org **Famai-motor** · แพ็ก free |
| SQL | `supabase/migrations/` 21 ไฟล์ (01–20) **รันครบทุกไฟล์แล้ว** — ตรวจตาม §9j ทุกตัว |
| ด่านตรวจ | `tools/qa/` 60 ชุด — `node tools/qa/run.js` (~20 นาที) |
| คู่มือผู้ใช้ | `docs/manual/` PDF สร้างด้วย `tools/manual/build.js` (พอร์ต 8123 ชนกับตัวรัน QA — ปิดตัวหนึ่งก่อน) |
| สำรองข้อมูล | `tools/backup/backup.js` (A2) — service key ผ่าน env เท่านั้น · `backups/` อยู่ใน .gitignore |
| งานค้าง/ticket | `.claude/issues/` — ตัวหลักคือ `001-live-sync-rest.md` (ช่องว่างโหมดข้อมูลจริง) |
| วิธีทำงานของโปรเจกต์ | skill `famai-ship` (ลำดับส่งงานหนึ่งรอบ) · `famai-mutation` (พิสูจน์ด่านให้แดงได้จริง) |
| งานอยู่บนกิ่ง | `claude/start-b18xi3` — push ทุก commit ห้ามเปิด PR เว้นแต่เจ้าของสั่ง |

---

## 2. บรีฟแก้ไขครั้งที่ 1 (13-14 ส.ค. 2569) — ส่งครบทั้ง 6 กลุ่ม

| กลุ่ม | ได้อะไร | ด่านตรวจ |
|---|---|---|
| รากฐาน | popup ยืนยันก่อนบันทึก (`confirmSave`) ทุกจุดเงิน/เอกสาร ยกเว้นปุ่มลงเวลา · toast ใหม่ 3 โทน · ชั้น write-through `dbUp` | brief-r11 |
| ② เงิน/เอกสาร | ดอกเบี้ยช่วงงวดต่อเจ้า (C1-C4) · ยอดกู้=ต้น+ดอกทั้งสัญญา (B2) · ยอดชำระ ณ วันออกรถ=ดาวน์−ส่วนลด (B1) · ของแถมหลายชิ้น+นอกคลัง (B7-B8) · ข้อมูลออกใบกำกับตอนเปิดขาย (B9) · แก้เอกสาร manual ยกเว้นข้อมูลลูกค้า/เลขที่ (D2) · การเงินตรวจก่อนออกใบกำกับ (G1-G3) · AR แดงหน้าแรก+ติ๊กชำระในดีล (G4-G5) | money-r16, found-r16 |
| ③ ลูกค้า | วันที่+คำอธิบายใต้แถบขั้น · สัญญาณช้า (E5) · ลูกค้าเท-คืนสต๊อก-ยกเลิกเอกสาร (E1) · บทบาท+หน้าฝ่ายบริการ checklist และรอบติดตาม 7-120 วัน (E2-E4) | care-r17 |
| ④ บริษัท/B2B | ชั้นบริษัทเหนือสาขา แก้จาก UI (A3/F5) · ใบกำกับดึงเลขภาษีจากบริษัทของสาขา (O4) · โอนข้ามบริษัทถูกบังคับเปิดบิลขายส่ง · ขายส่งหลายคัน+ราคาแยกชุด (F1-F3) · **ตารางสิทธิ์ role×section แอดมินติ๊กเอง (J3)** ด่านทุกตัวอ่านจาก matrix | org-r18, perms-r18 |
| ⑤ พนักงาน | ลงเวลาเขียน RPC `punch_clock` จริง+รูปขึ้น bucket `hr-photo` (H1) · ใบลามีเหตุผล/หลักฐาน/ผู้ตัดสิน (K10/H6) · โควตาวันลาตั้งเอง (H3) · ขอออกนอกสถานที่ล่วงหน้า (H4) · ปฏิทินบริษัทเปิดทุกคน+วันหยุด (H5) · ศูนย์ซ่อมเลือกวันที่ (K9) ป้ายยอดชำระ (K13) | hr-r19 |
| ① หน้าตา | ยกเลิกกฎสีเดียว (§9k) · เมนู 6 กลุ่มสี · ขั้นตอนสีเขียว · ธีม 4 แบบรวมโหมดมืด · ขนาดอักษร 3 ระดับ · ฟอนต์ไทย 20 ตัว · เก็บต่อคนต่อเครื่อง (I1-I6) | theme-r20 |
| ⑥ แจ้งเตือน+เก็บงาน | ศูนย์แจ้งเตือนคิดสดจากระเบียนจริง+สรุปประจำวันผู้บริหาร (J1-J2 — อีเมล/LINE เป็นโครง ยังไม่ส่งจริงตามคำสั่ง) · รูปรถในหน้าขาย/ดีล/ศูนย์ซ่อม (K2) · สคริปต์สำรองข้อมูล (A2) | notif-r21 |

หลักที่ใช้ทุกกลุ่ม: ด่านอยู่ในฟังก์ชันเขียนไม่ใช่ปุ่ม (§9b) · ด่านใหม่พิสูจน์แบบ mutation ทุกตัว ·
migration ยิงตรวจด้วยการสวมบทบาทจริง (§9j) · ผลการตัดสินแช่ลงหลักฐาน (§9i)

---

## 3. สิ่งที่ต้องกดเองบนหน้าเว็บ Supabase (O5 — เจ้าของรับไว้เอง ยังไม่ได้ทำ)

| # | เรื่อง | ทำไมสำคัญ |
|---|---|---|
| 1 | **เปลี่ยนรหัสผ่านฐานข้อมูล** — Settings → Database → Reset database password | รหัสเดิมถูกส่งผ่านแชต ถือว่าเปิดเผยแล้ว |
| 2 | **เปิด Leaked Password Protection** — Authentication → Policies | advisor เตือนอยู่ |
| 3 | **เติม `branch.phone`** ทั้ง 3 สาขา | หน้าติดตามลูกค้าบอก "กรุณาติดต่อร้าน" แต่ไม่มีเบอร์ให้โทร |

---

## 4. ของที่รอจากเจ้าของ (ระบบออกแบบเผื่อไว้แล้ว ใส่ค่าจริงได้ทันที)

- รูปถ่ายรถตัวอย่าง 3 รูป + ไฟล์โลโก้ (D3) — ช่อง `CFG.logo` มีแล้ว
- อัตราดอกเบี้ยจริงต่อเจ้าต่อช่วงงวด (C1) — แก้ได้เองที่ ตั้งค่า → บริษัทไฟแนนซ์
- เงื่อนไขเครดิตขายส่ง (F4) — คอลัมน์ `due_at` เตรียมไว้แล้ว
- เลขผู้เสียภาษีจริงของอีก 2 บริษัท (A3) — แก้ได้เองที่ ตั้งค่า → บริษัทและสาขา

---

## 5. ช่องว่างของโหมดข้อมูลจริง (รายละเอียดใน `.claude/issues/001-live-sync-rest.md`)

เขียนขึ้นฐานข้อมูล (write-through) **ครบทุกตารางธุรกรรมแล้ว (v1.49)** แต่**การโหลดกลับ**ยังจำกัด:
โหมดจริงเริ่มรายการขาย/ใบลา/บิลว่างเสมอ (แนวทาง "ข้อมูลจริงมีแต่รถ") — ก้าวถัดไปที่ใหญ่ที่สุดคือ
โหลด `STAFF` จริงจาก `app_user`/`employee` แล้วตามด้วยรายการย้อนหลังทุกตาราง ·
เลขเอกสารยังออกจากตัวนับในเครื่อง ต้องย้ายไป `next_doc_no()` ก่อนใช้หลายเครื่องพร้อมกัน ·
รูปใน bucket ส่วนตัวดูข้ามเครื่องต้องเพิ่ม signed URL

---

## 6. กติกาที่ห้ามลืม

- อ่าน `.claude/CLAUDE.md` + `.claude/rules/` ก่อนเริ่ม — กับดักสภาพแวดล้อมเขียนไว้ครบ
- `node tools/qa/suites/syntax.js` ก่อนเสมอ · เส้นฐาน 31 ส.ค. 2569: **เขียวครบ 80/80 ชุด**
- ห้าม commit: รหัสผ่านฐานข้อมูล · `SUPABASE_SERVICE_ROLE_KEY` · PAT — publishable key commit ได้
- แก้ schema = เพิ่มไฟล์ migration ใหม่เสมอ แล้วยิงตรวจด้วยสิทธิ์จริงตาม §9j
